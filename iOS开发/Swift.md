```swift
// 字符串插值
print("hello \(2 + 3) world") // hello 5 world

// 声明常量
let a: Int = 1

// 声明变量
var a: Int = 1

// 生成1个0-5之间的随机整数
Int.random(in: 0 ... 5)
Int.random(in: 0 ..< 6)

// 函数
func funcName(text: String) {
	// do something
	print(text)
}
funcName(text: "hello world")
```